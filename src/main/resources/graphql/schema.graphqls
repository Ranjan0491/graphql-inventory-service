type Query {
    getItems: [Item]
    getItemById(id: ID): Item
}

type Item {
    id: ID
    category: ItemCategory!
    name: String!
    price: Float!
}

type ItemCategory {
    id: ID
    name: String!
}